Investigating solutions to optimize drivebase and vision code for quicker build season set up.

Idea:
  - Added DrivebaseConfig to serve as a constrant list for drivebase variable, while the drivebase code can remain unchanged.
  - Standalone PoseEstimater system instead of inside SubDrivebase
